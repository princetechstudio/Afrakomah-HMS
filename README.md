# Afrakomah HMS

## Supabase setup

The app stores each HMS collection as separate records in Supabase. Run [`supabase/migration.sql`](supabase/migration.sql) in the Supabase SQL Editor. The local `.env.local` file contains the project URL and public client key; do not use a service-role key in the browser.
# afrakomah2
management system
